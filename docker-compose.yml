version: '2'
services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - 'resin-data:/data'
    command: "gunicorn 'server:app' -b 0.0.0.0:80"

  worker:
    command: "python worker.py" 
    volumes:
      - 'resin-data:/data'

volumes:
    resin-data:
